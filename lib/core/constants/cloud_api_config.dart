class CloudApiConfig {
  static const endpoint = 'https://gaza-c.online/api.php';

  // The inspected API worked without a token. Leave empty unless the
  // private API is configured to require one.
  static const token = '';
}