name: citizen_registry
description: "سجل المواطنين - local read-only citizen registry"
publish_to: "none"
version: 0.1.0+1
environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.38.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  file_picker: ^10.3.3
  path: ^1.9.1
  path_provider: ^2.1.5
  intl: ^0.20.2
  shared_preferences: ^2.5.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  generate: true
